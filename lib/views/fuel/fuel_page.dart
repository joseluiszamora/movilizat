import 'package:flutter/material.dart';

class FuelPage extends StatelessWidget {
  const FuelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("FuelPage");
  }
}
